---
tags:
  - Linux
  - Kernel
  - 同步
created: 2026-03-11
status: 待学习
---

# 自旋锁

> [!info] 目标
> 理解Linux内核自旋锁的实现与使用

## 自旋锁概述

自旋锁（Spinlock）是一种忙等待锁：
- 获取失败时循环等待（自旋）
- 不进行上下文切换
- 适用于短时间持有

## 基本使用

```c
#include <linux/spinlock.h>

// 定义锁
spinlock_t my_lock;

// 初始化
spin_lock_init(&my_lock);

// 使用
spin_lock(&my_lock);
// 临界区
spin_unlock(&my_lock);
```

## 自旋锁变种

### 普通自旋锁

```c
void spin_lock(spinlock_t *lock);
void spin_unlock(spinlock_t *lock);
```

### 禁用中断的自旋锁

```c
unsigned long flags;

// 保存中断状态并禁用中断
spin_lock_irqsave(&lock, flags);
// 临界区
spin_unlock_irqrestore(&lock, flags);

// 或直接禁用中断
spin_lock_irq(&lock);
spin_unlock_irq(&lock);
```

### 禁用底半部的自旋锁

```c
spin_lock_bh(&lock);   // 禁用软中断
spin_unlock_bh(&lock);
```

## 选择指南

| 场景 | 推荐使用 |
|------|----------|
| 进程上下文，无中断 | spin_lock |
| 进程上下文，需保护中断数据 | spin_lock_irqsave |
| 中断上下文 | spin_lock |
| 软中断上下文 | spin_lock_bh |

## 自旋锁实现

### 数据结构

```c
typedef struct spinlock {
    union {
        struct raw_spinlock rlock;

#ifdef CONFIG_DEBUG_LOCK_ALLOC
        struct {
            u8 __padding;
        };
#endif
    };
} spinlock_t;

typedef struct raw_spinlock {
    arch_spinlock_t raw_lock;
#ifdef CONFIG_DEBUG_SPINLOCK
    unsigned int magic, owner_cpu;
    void *owner;
#endif
} raw_spinlock_t;
```

### x86实现

```c
// 简化的自旋锁实现
static inline void arch_spin_lock(arch_spinlock_t *lock)
{
    while (atomic_xchg(&lock->val, 1) == 1) {
        // 等待锁释放
        while (READ_ONCE(lock->val) == 1)
            cpu_relax();  // PAUSE指令
    }
}

static inline void arch_spin_unlock(arch_spinlock_t *lock)
{
    WRITE_ONCE(lock->val, 0);
}
```

## 读写自旋锁

```c
#include <linux/rwlock.h>

rwlock_t my_rwlock;

// 读锁（可共享）
read_lock(&my_rwlock);
// 读操作
read_unlock(&my_rwlock);

// 写锁（独占）
write_lock(&my_rwlock);
// 写操作
write_unlock(&my_rwlock);

// 带中断保存的版本
read_lock_irqsave(&lock, flags);
read_unlock_irqrestore(&lock, flags);
```

## 序列锁（Seqlock）

适用于读多写少的场景：

```c
#include <linux/seqlock.h>

seqlock_t my_seqlock;

// 写操作
write_seqlock(&my_seqlock);
// 写入数据
write_sequnlock(&my_seqlock);

// 读操作
unsigned int seq;
do {
    seq = read_seqbegin(&my_seqlock);
    // 读取数据
} while (read_seqretry(&my_seqlock, seq));
```

## 注意事项

> [!warning] 自旋锁使用规则
> 1. **不能睡眠**：持有自旋锁时不能调用可能睡眠的函数
> 2. **持锁时间短**：长时间持有会浪费CPU
> 3. **避免死锁**：不要递归获取同一把锁

## 调试支持

```bash
# 启用锁调试
CONFIG_DEBUG_SPINLOCK=y
CONFIG_DEBUG_LOCK_ALLOC=y

# 检测死锁、非法释放等问题
```

## 相关笔记
- [[原子操作]]
- [[互斥锁与信号量]]
- [[RCU机制]]
